# Інструкція для запуску проєкту

## Крок 1: Підготовка середовища

### Windows:
```bash
# Перевірте версію Python (потрібна 3.8+)
py --version

# Створіть віртуальне середовище
py -m venv .venv

# Активуйте віртуальне середовище
.venv\Scripts\activate.bat
```

### Linux/Mac:
```bash
# Перевірте версію Python
python3 --version

# Створіть віртуальне середовище
python3 -m venv .venv

# Активуйте віртуальне середовище
source .venv/bin/activate
```

## Крок 2: Встановлення залежностей

```bash
# Оновіть pip
python.exe -m pip install --upgrade pip
# або для Linux/Mac:
python3 -m pip install --upgrade pip

# Встановіть всі залежності
pip install -r requirements.txt
```

## Крок 3: Налаштування бази даних

### Варіант 1: PostgreSQL (рекомендовано)

1. Встановіть PostgreSQL на вашому комп'ютері
2. Створіть базу даних:
```sql
CREATE DATABASE pd421_django_mvt;
```

3. Перевірте налаштування в `djangomvt/mysite/settings.py`:
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'HOST': 'localhost',
        'NAME': 'pd421_django_mvt',
        'USER': 'postgres',
        'PASSWORD': '123456',  # Змініть на ваш пароль
        'PORT': '5432'
    }
}
```

### Варіант 2: SQLite (для швидкого старту)

Якщо хочете використовувати SQLite, розкоментуйте в `settings.py`:
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```
І закоментуйте налаштування PostgreSQL.

## Крок 4: Міграції бази даних

```bash
# Перейдіть до директорії проєкту
cd djangomvt

# Створіть міграції
py manage.py makemigrations
# або
python manage.py makemigrations

# Застосуйте міграції
py manage.py migrate
# або
python manage.py migrate
```

## Крок 5: Створення суперкористувача (опціонально)

```bash
py manage.py createsuperuser
# або
python manage.py createsuperuser
```

Введіть:
- Username: admin (або будь-яке інше)
- Email: ваш email
- Password: ваш пароль

## Крок 6: Запуск сервера

```bash
# Запустіть сервер розробки
py manage.py runserver
# або
python manage.py runserver

# Або на конкретному порту
py manage.py runserver 8000
```

Сервер буде доступний за адресою: http://127.0.0.1:8000/

## Доступні сторінки:

- **Головна**: http://127.0.0.1:8000/
- **Категорії**: http://127.0.0.1:8000/categories/
- **Додати категорію**: http://127.0.0.1:8000/categories/add/
- **Реєстрація**: http://127.0.0.1:8000/users/register/
- **Вхід**: http://127.0.0.1:8000/users/login/
- **Відновлення пароля**: http://127.0.0.1:8000/users/password-reset/
- **Адмін панель**: http://127.0.0.1:8000/admin/

## Структура проєкту:

```
djangomvt/
├── categories/          # Додаток категорій
├── users/              # Додаток користувачів
├── mysite/             # Налаштування проєкту
├── templates/          # Глобальні шаблони
├── static/             # Статичні файли (CSS, JS)
├── images/             # Зображення категорій
├── media/              # Завантажені файли користувачів
└── manage.py           # Скрипт управління Django
```

## Можливі проблеми та рішення:

### Помилка: "No module named 'psycopg2'"
**Рішення**: Встановіть `pip install psycopg2-binary`

### Помилка: "No module named 'PIL'"
**Рішення**: Встановіть `pip install Pillow`

### Помилка: "No module named 'unidecode'"
**Рішення**: Встановіть `pip install Unidecode`

### Помилка підключення до бази даних
**Рішення**: 
- Перевірте, чи запущений PostgreSQL
- Перевірте правильність налаштувань в `settings.py`
- Переконайтеся, що база даних існує

### Помилка з міграціями
**Рішення**: 
```bash
# Видаліть файли міграцій (крім __init__.py) в папках migrations/
# Потім виконайте:
py manage.py makemigrations
py manage.py migrate
```

## Деактивація віртуального середовища

Коли закінчите роботу:
```bash
deactivate
```

---

**Успіхів у розробці! 🚀**


